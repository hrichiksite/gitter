replication:
  replSetName: troupeSet

storage:
  engine: wiredTiger
  dbPath: /usr/local/var/mongodb/r1
  journal:
    enabled: true

net:
  bindIp: 127.0.0.1
  port: 27018

operationProfiling:
  slowOpThresholdMs: 100
  mode: slowOp
